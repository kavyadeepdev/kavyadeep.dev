---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<main
  class:list={[
    "min-h-full flex-1 w-full flex justify-center bg-white overflow-y-auto px-4 sm:px-6 md:px-0",
    className,
  ]}
>
  <slot />
</main>
